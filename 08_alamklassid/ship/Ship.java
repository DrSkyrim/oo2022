public class Ship{
    String name, module;
    int length,height,width;

public Ship(String new_name, int new_length,int new_height,int new_width, String new_module){
    name=new_name;
    length=new_length;
    height=new_height;
    width=new_width;
    module=new_module;
}
}